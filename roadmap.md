# Roadmap

1. Hostinger folder-upload deployment
   - Build a static Hostinger-compatible version
   - Replace server-dependent workflows with authenticated database operations
   - Add direct-route rewrite configuration
   - Package and test the upload-ready output

2. (Done) Admin Database console added — shows all tables live from DB.
