# Roadmap

1. Hostinger folder-upload deployment
   - Configure a Hostinger Node-compatible Nitro build
   - Preserve authenticated server workflows and keep private credentials server-side
   - Package the production server and static assets as an upload-ready archive
   - Document Hostinger Node 22, start command, domain, and environment settings

2. (Done) Admin Database console added — shows all tables live from DB.
